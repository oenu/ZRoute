openapi: 3.1.0
info:
  title: ZRoute API
  version: 0.1.0
  description: API for logical mapping of transit systems
  license:
    identifier: GPL-3.0-or-later
    name: GPL-3.0-or-later
servers:
  - url: http://localhost:8080/api/v1
    description: Development server
  - url: https://api.zroute.example.com/api/v1
    description: Production server
paths:
  /health:
    $ref: './paths/_index.yaml#/health'
  /example:
    $ref: './paths/_index.yaml#/example'
components:
  schemas:
    $ref: './components/schemas/_index.yaml'
